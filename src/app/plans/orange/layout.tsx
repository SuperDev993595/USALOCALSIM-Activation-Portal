import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Orange ULTRA eSIM Data Plans | USALOCALSIM",
  description:
    "Orange ULTRA eSIM plans — Europe (41 countries) and World zone roaming. Official Orange bundles, eSIM only.",
};

export default function OrangePlansLayout({ children }: { children: React.ReactNode }) {
  return children;
}
